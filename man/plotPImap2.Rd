% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPImap2.R
\name{plotPImap2}
\alias{plotPImap2}
\title{Enhanced Person-Item Map Plotting Function}
\usage{
plotPImap2(
  object,
  item.subset = "all",
  sorted = FALSE,
  main = "Person-Item Map",
  latdim = "Latent Dimension",
  pplabel = "Person\\nParameter\\nDistribution",
  cex.gen = 0.7,
  xrange = NULL,
  warn.ord = TRUE,
  warn.ord.colour = "black",
  irug = TRUE,
  pp = NULL,
  margins = c(2.5, 4, 0, 1)
)
}
\arguments{
\item{object}{An eRm model object (RM, RSM, or PCM). LLTM, LRSM, and LPCM are not supported.}

\item{item.subset}{Character vector specifying which items to include. Use "all" for all items,
or provide a vector of item names/indices. Default is "all".}

\item{sorted}{Logical. Should items be sorted by difficulty? Default is FALSE.}

\item{main}{Character string for the main title. Default is "Person-Item Map".}

\item{latdim}{Character string for the x-axis label. Default is "Latent Dimension".}

\item{pplabel}{Character string for the person parameter distribution label.
Default is "Person\nParameter\nDistribution".}

\item{cex.gen}{Numeric value for general text size scaling. Default is 0.7.}

\item{xrange}{Numeric vector of length 2 specifying x-axis range. If NULL (default),
range is calculated automatically.}

\item{warn.ord}{Logical. Should warnings for disordered thresholds be displayed? Default is TRUE.}

\item{warn.ord.colour}{Character string specifying color for disordered threshold warnings.
Default is "black".}

\item{irug}{Logical. Should item threshold rug be displayed? Default is TRUE.}

\item{pp}{A person.parameter object. If NULL (default), it will be calculated from the model.}

\item{margins}{Numeric vector of length 4 specifying plot margins (bottom, left, top, right).
Default is c(2.5, 4, 0, 1).}
}
\value{
No return value. Function creates a plot.
}
\description{
An enhanced version of the eRm plotPImap function with additional customization options
and improved visualization features for Rasch model analysis.
}
\details{
This function creates a person-item map showing the distribution of person abilities
and item difficulties on the same scale. It displays:
\itemize{
\item Person parameter distribution (top panel)
\item Item thresholds and difficulties (bottom panel)
\item Warnings for disordered thresholds (if enabled)
}

The function only works with RM, RSM, and PCM models from the eRm package.
}
\examples{
\dontrun{
# Basic person-item map
plotPImap2(rasch_model)

# Customized map with sorted items
plotPImap2(rasch_model, sorted = TRUE, main = "My REBL Analysis")

# Map with specific item subset
plotPImap2(rasch_model, item.subset = c("item1", "item5", "item10"))
}
}
